export default class PixiShaderSmoke {
    container: HTMLElement;
    width: number;
    height: number;
    smokeHeight: string;
    constructor({ container, height }: {
        container: HTMLElement;
        height: string;
    });
    init(): void;
}
